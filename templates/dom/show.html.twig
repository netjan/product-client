{% extends 'base.html.twig' %}

{% block title %}Dom{% endblock %}

{% block body %}
    <h1>Dom</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ dom.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ dom.name }}</td>
            </tr>
            <tr>
                <th>Qty</th>
                <td>{{ dom.qty }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_dom_index') }}">back to list</a>

    <a href="{{ path('app_dom_edit', {'id': dom.id}) }}">edit</a>

    {{ include('dom/_delete_form.html.twig') }}
{% endblock %}
