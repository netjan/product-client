<form method="post" action="{{ path('app_dom_delete', {'id': dom.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ dom.id) }}">
    <button class="btn">Delete</button>
</form>
